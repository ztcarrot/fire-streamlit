# Streamlit Cloud 部署指南

## 自动部署步骤

### 方法 1: 通过 Streamlit Cloud 网站部署

1. **访问 Streamlit Cloud**
   打开浏览器,访问: https://share.streamlit.io

2. **登录 GitHub**
   - 点击 "Sign in with GitHub"
   - 如果已登录,会自动跳转

3. **创建新应用**
   - 点击 "New app" 按钮
   - 选择您的仓库: `ztcarrot/fire-streamlit`
   - 分支: `master`
   - 主文件路径: `app.py`
   - 点击 "Deploy"

4. **等待部署**
   Streamlit Cloud 会自动:
   - 安装 requirements.txt 中的依赖
   - 启动应用
   - 分配 URL

5. **访问应用**
   部署完成后,您的应用 URL 将是:
   `https://fire-streamlit.streamlit.app`

### 方法 2: 使用 GitHub CLI 自动部署 (如果可用)

```bash
gh repo view fire-streamlit --web
```

然后按照网页提示操作。

## 验证部署

部署完成后,检查以下功能:
- ✓ 页面正常加载
- ✓ 可以输入参数并计算
- ✓ 图表正常显示
- ✓ 预设管理功能正常
- ✓ 多场景对比功能正常
- ✓ Excel 导出功能正常

## 更新 README

部署成功后,将应用链接添加到 README.md:

```markdown
## 在线演示

🚀 **在线体验**: https://fire-streamlit.streamlit.app
```

## 故障排除

如果部署失败:
1. 检查 requirements.txt 格式是否正确
2. 检查 app.py 是否在根目录
3. 查看 Streamlit Cloud 的部署日志
